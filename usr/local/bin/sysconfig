#!/usr/bin/env python3
"""
StormOS Utilities Program v5.1 - KDE/Plasma Optimized
Works flawlessly on KDE, GNOME, XFCE.
Uses konsole (KDE) or gnome-terminal with correct --hold/--keep semantics.
"""

import os
import shutil
import subprocess
import sys

# Ensure GTK 3 is used
os.environ['GI_TYPELIB_PATH'] = '/usr/lib/girepository-1.0'

import gi
gi.require_version('Gtk', '3.0')
from gi.repository import Gtk


# ============================================================
# DETECT DESKTOP ENVIRONMENT
# ============================================================
def is_kde() -> bool:
    """Return True if running under KDE Plasma."""
    desktop = os.environ.get('XDG_CURRENT_DESKTOP', '').lower()
    return 'kde' in desktop or 'plasma' in desktop


# ============================================================
# TERMINAL HANDLING – KDE FIRST
# ============================================================
def find_terminal():
    """
    Return path to available terminal emulator.
    - On KDE: try konsole first, then fallback.
    - On other DE: gnome-terminal, xfce4-terminal, xterm.
    """
    if is_kde():
        terminals = ["konsole", "gnome-terminal", "xfce4-terminal", "xterm"]
    else:
        terminals = ["gnome-terminal", "xfce4-terminal", "konsole", "xterm"]
    for term in terminals:
        path = shutil.which(term)
        if path:
            return path
    return None


def needs_terminal(command: str) -> bool:
    """Determine if a command should be run inside a terminal."""
    if command.startswith("xdg-open") or command.startswith("eom"):
        return False
    if command in ("xfce4-session-logout", "/usr/bin/octopi %U", "alltray -H sysconfig"):
        return False
    if command in ("upkeyring", "upsystem", "tinstall", "minstaller", "trayinjector", "utilityup"):
        return True
    if "pacman" in command or "yay" in command or "reflector" in command or "systemctl" in command:
        return True
    if "lshw" in command or "top" in command:
        return True
    return True


def run_command(command_str: str, keep_terminal: bool = True):
    """
    Execute a command string.
    - For sudo commands, uses pkexec if available (graphical password dialog).
    - For commands that need a terminal, opens the appropriate terminal emulator.
    - For GUI/URL commands, runs directly without terminal.
    """
    # Direct execution (no terminal)
    if not needs_terminal(command_str):
        try:
            subprocess.Popen(command_str, shell=True, start_new_session=True,
                             stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
            return True
        except Exception as e:
            error_dialog = Gtk.MessageDialog(parent=None,
                                             flags=Gtk.DialogFlags.MODAL,
                                             type=Gtk.MessageType.ERROR,
                                             buttons=Gtk.ButtonsType.OK,
                                             message_format=f"Failed to run command:\n{command_str}\n\n{str(e)}")
            error_dialog.run()
            error_dialog.destroy()
            return False

    # Terminal commands: use pkexec for sudo if available
    if command_str.startswith("sudo ") and shutil.which("pkexec"):
        command_str = command_str.replace("sudo ", "pkexec ", 1)

    term = find_terminal()
    if not term:
        error_dialog = Gtk.MessageDialog(parent=None,
                                         flags=Gtk.DialogFlags.MODAL,
                                         type=Gtk.MessageType.ERROR,
                                         buttons=Gtk.ButtonsType.OK,
                                         message_format="No terminal found.\nInstall konsole (KDE) or gnome-terminal.")
        error_dialog.run()
        error_dialog.destroy()
        return False

    term_name = os.path.basename(term)
    # Build the shell command that will be executed inside the terminal
    if keep_terminal:
        shell_cmd = f'bash -c "{command_str}; echo; echo \'Press Enter to close...\'; read"'
    else:
        shell_cmd = f'bash -c "{command_str}"'

    # Terminal-specific arguments
    if term_name == "konsole":
        # konsole: --hold keeps window open after command finishes
        args = [term, "--hold", "-e", shell_cmd]
    elif term_name == "gnome-terminal":
        args = [term, "--title", "StormOS Utility", "--", "bash", "-c", shell_cmd]
    elif term_name == "xfce4-terminal":
        args = [term, "--disable-server", "--title", "StormOS Utility", "--command", shell_cmd]
    elif term_name == "xterm":
        args = [term, "-title", "StormOS Utility", "-e", shell_cmd]
    else:
        args = [term, "-e", shell_cmd]

    try:
        subprocess.Popen(args, start_new_session=True,
                         stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
        return True
    except Exception as e:
        error_dialog = Gtk.MessageDialog(parent=None,
                                         flags=Gtk.DialogFlags.MODAL,
                                         type=Gtk.MessageType.ERROR,
                                         buttons=Gtk.ButtonsType.OK,
                                         message_format=f"Failed to launch terminal:\n{str(e)}")
        error_dialog.run()
        error_dialog.destroy()
        return False


# ============================================================
# MAIN WINDOW (unchanged – button commands remain the same)
# ============================================================
class MainWindow(Gtk.Window):
    def __init__(self):
        Gtk.Window.__init__(self, title="StormOS Utilities Program v5.1")
        self.set_border_width(10)
        self.set_default_size(600, 400)

        hbox = Gtk.Box(orientation=Gtk.Orientation.HORIZONTAL, spacing=6)
        self.add(hbox)

        notebook_vbox = Gtk.Box(orientation=Gtk.Orientation.VERTICAL, spacing=6)
        hbox.pack_start(notebook_vbox, True, True, 0)

        notebook = Gtk.Notebook()
        notebook.set_tab_pos(Gtk.PositionType.LEFT)
        notebook_vbox.pack_start(notebook, True, True, 0)

        self.create_maintenance_tab(notebook)
        self.create_game_utilities_tab(notebook)
        self.create_printer_tab(notebook)
        self.create_arch_university_tab(notebook)
        self.create_about_us_tab(notebook)

    def create_button_with_icon(self, label, command, icon_name):
        button = Gtk.Button()
        box = Gtk.Box(orientation=Gtk.Orientation.HORIZONTAL)
        image = Gtk.Image.new_from_icon_name(icon_name, Gtk.IconSize.BUTTON)
        label_widget = Gtk.Label(label=label)
        box.pack_start(image, False, False, 5)
        box.pack_start(label_widget, False, False, 5)
        button.add(box)
        button.connect("clicked", self.run_command, command)
        return button

    def run_command(self, widget, command):
        run_command(command)

    # ----- All tab creation methods remain exactly as before -----
    def create_maintenance_tab(self, notebook):
        grid = Gtk.Grid()
        grid.set_column_homogeneous(True)
        grid.set_row_homogeneous(True)

        commands = [
            ("Step 1. Refresh Mirrors", "sudo reflector --verbose -l 20 --sort rate --save /etc/pacman.d/mirrorlist", "view-refresh"),
            ("Step 2. System Updates", "sudo pacman -Syyu --noconfirm", "system-software-update"),
            ("Step 3. (Optional) Aur Updates", "yay -Syyu --noconfirm", "system-software-update"),
            ("Step 4. (Optional) Arch Keyring Updater", "upkeyring", "system-lock-screen"),
            ("Step 4a. (Optional) Renew Arch Keyring", "upsystem", "system-lock-screen"),
            ("Step 5. Install Teamviewer", "tinstall", "applications-internet"),
            ("Step 6. Install Lshw", "sudo pacman -S lshw --noconfirm", "applications-system"),
            ("Step 7. Install i2c-tools for decode-dimms", "sudo pacman -S i2c-tools --noconfirm", "applications-system"),
            ("Step 8. Install Nvidia drivers (dkms) after that, (Reboot)", "sudo pacman -S nvidia-open-dkms lib32-nvidia-utils lib32-opencl-nvidia lib32-primus_vk lib32-libvdpau cuda-tools cuda opencl-nvidia primus_vk --noconfirm", "video-display"),
            ("Step 9. Install Nvidia-390xx drivers (dkms) after that (Reboot)", "sudo pacman -S nvidia-390xx-dkms nvidia-390xx-utils opencl-nvidia-390xx --noconfirm", "video-display")
        ]

        for i, (label, command, icon_name) in enumerate(commands):
            button = self.create_button_with_icon(label, command, icon_name)
            grid.attach(button, 0, i, 1, 1)

        label = Gtk.Label(label="<b>Maintenance/Updates/Support</b>", use_markup=True)
        notebook.append_page(grid, label)

    def create_game_utilities_tab(self, notebook):
        grid = Gtk.Grid()
        grid.set_column_homogeneous(True)
        grid.set_row_homogeneous(True)

        commands = [
            ("Install Steam Native", "sudo pacman -S --noconfirm steam-native-runtime gamemode", "applications-games"),
            ("Install Heroic Launcher", "yay -S --noconfirm heroic-games-launcher-bin gamemode", "applications-games"),
            ("Install Lutris Launcher", "sudo pacman -S --noconfirm lutris gamemode", "applications-games"),
            ("Install ProtonGE Updater", "yay -S --noconfirm proton-community-updater", "applications-games"),
            ("Install Mangohud/Goverlay", "yay -S --noconfirm mangohud goverlay-bin", "applications-games"),
            ("Install Bottles Launcher", "yay -S --noconfirm bottles", "applications-games"),
            ("Install Warpinator", "sudo pacman -S warpinator --noconfirm", "applications-internet"),
            ("Install Gnome-Calculator", "sudo pacman -S gnome-calculator --noconfirm", "accessories-calculator"),
            ("Install Flameshot", "sudo pacman -S flameshot --noconfirm", "accessories-screenshot"),
            ("Install Transmission-Gtk", "sudo pacman -S transmission-gtk --noconfirm", "network-workgroup"),
            ("Install Thunderbird", "sudo pacman -S thunderbird --noconfirm", "internet-mail"),
            ("Install Xed (Alt) Text Editor", "sudo pacman -S xed --noconfirm", "accessories-text-editor"),
            ("Install Only Office", "yay -S onlyoffice-bin --noconfirm", "applications-office"),
            ("Install Media Stream Installer", "minstaller", "multimedia-video-player"),
            ("Install Minimize to Tray (Auto Reboot)", "trayinjector", "system-run")
        ]

        for i, (label, command, icon_name) in enumerate(commands):
            button = self.create_button_with_icon(label, command, icon_name)
            grid.attach(button, 0, i, 1, 1)

        label = Gtk.Label(label="<b>Game/Utilities/Scripts</b>", use_markup=True)
        notebook.append_page(grid, label)

    def create_printer_tab(self, notebook):
        grid = Gtk.Grid()
        grid.set_column_homogeneous(True)
        grid.set_row_homogeneous(True)

        commands = [
            ("Enable Cups/Printer", "systemctl enable --now cups", "printer"),
            ("Cups/Printer Web Interface", "xdg-open http://127.0.0.1:631", "applications-internet"),
            ("(Optional) Epson Drivers", "epsoninstaller", "printer"),
            ("HP Drivers (Preinstalled)", "eom", "printer")
        ]

        for i, (label, command, icon_name) in enumerate(commands):
            button = self.create_button_with_icon(label, command, icon_name)
            grid.attach(button, 0, i, 1, 1)

        label = Gtk.Label(label="<b>Cups/Printer Drivers</b>", use_markup=True)
        notebook.append_page(grid, label)

    def create_arch_university_tab(self, notebook):
        grid = Gtk.Grid()
        grid.set_column_homogeneous(True)
        grid.set_row_homogeneous(True)

        commands = [
            ("StormOS Arch Commands", '/usr/local/bin/data/commands', "utilities-terminal"),
            ("Visit The Arch Wiki", "xdg-open https://wiki.archlinux.org/", "internet-web-browser"),
            ("Visit The Arch Website", "xdg-open https://archlinux.org/", "internet-web-browser"),
            ("Explanation of Arch Commands", "xdg-open https://www.geeksforgeeks.org/pacman-command-in-arch-linux/#:~:text=Pacman%20is%20a%20package%20manager,a%20text%2Dbased%20package%20database.", "internet-web-browser"),
            ("Visit 3rd Party Software for Arch AUR Website", "xdg-open https://aur.archlinux.org/", "internet-web-browser"),
            ("Beginner Video Tutorial on Pacman", "xdg-open https://www.youtube.com/watch?v=TQaHfQrwnXo", "applications-multimedia"),
            ("Novice/Advanced Video Tutorial on Pacman", "xdg-open https://www.youtube.com/watch?v=-dEuXTMzRKs", "applications-multimedia")
        ]

        for i, (label, command, icon_name) in enumerate(commands):
            button = self.create_button_with_icon(label, command, icon_name)
            grid.attach(button, 0, i, 1, 1)

        self.add_left_buttons(grid, len(commands))

        label = Gtk.Label(label="<b>Arch University</b>", use_markup=True)
        notebook.append_page(grid, label)

    def create_about_us_tab(self, notebook):
        grid = Gtk.Grid()
        grid.set_column_homogeneous(True)
        grid.set_row_homogeneous(True)

        commands = [
            ("Install Discord", "sudo pacman -S discord --noconfirm", "internet-chat"),
            ("Visit us and Join us in The StormOS Channel", "xdg-open https://discord.gg/7Ze98XKJVm", "internet-web-browser"),
            ("Distrowatch", "xdg-open https://distrowatch.com", "internet-web-browser"),
            ("Paypal Page", "xdg-open https://www.paypal.com/paypalme/fitzpatrick251", "internet-web-browser"),
            ("Patreon Page", "xdg-open https://patreon.com/benfitzpatrick", "internet-web-browser"),
            ("Visit The StormOS Site", "xdg-open https://techcafe757.wixsite.com/storm-os", "internet-web-browser"),
            ("StormOS ReadMe", '/usr/local/bin/data/about', "text-x-generic")
        ]

        for i, (label, command, icon_name) in enumerate(commands):
            button = self.create_button_with_icon(label, command, icon_name)
            grid.attach(button, 0, i, 1, 1)

        label = Gtk.Label(label="<b>About Us/Donations/Merchandise</b>", use_markup=True)
        notebook.append_page(grid, label)

    def add_left_buttons(self, grid, start_index):
        buttons = [
            ("Logout", "xfce4-session-logout", "system-log-out"),
            ("System Info by Lshw", "sudo lshw -short", "system-help"),
            ("System Resources via Top", "top", "utilities-system-monitor"),
            ("Update Utility Program", "utilityup", "view-refresh"),
            ("Add/Remove Software", "/usr/bin/octopi %U", "system-software-install"),
            ("Add Utility Tool to Tray", "alltray -H sysconfig", "utilities-terminal")
        ]

        for i, (label, command, icon_name) in enumerate(buttons):
            button = self.create_button_with_icon(label, command, icon_name)
            grid.attach(button, 0, start_index + i, 1, 1)


def main():
    win = MainWindow()
    win.connect("destroy", Gtk.main_quit)
    win.show_all()
    Gtk.main()


if __name__ == "__main__":
    main()
